"use strict";
(function(root){
 const money=value=>{const n=BigInt(value||0),a=n<0n?-n:n;return (n<0n?'−':'')+'₹'+(a/100n).toLocaleString('en-IN')+'.'+String(a%100n).padStart(2,'0');};
 const card=(el,label,value,hint)=>{const c=el('article',undefined,'card admin-kpi-compact');c.append(el('span',label,'admin-kpi-label'),el('strong',String(value)),el('small',hint));return c;};
 const button=(el,label,fn,cls='')=>{const b=el('button',label,cls);b.type='button';b.onclick=fn;return b;};
 const table=(el,headers,rows)=>{const wrap=el('div',undefined,'table-wrap'),t=el('table',undefined,'admin-table'),head=el('thead'),hr=el('tr');for(const h of headers)hr.append(el('th',h));head.append(hr);t.append(head);const body=el('tbody');for(const row of rows){const tr=el('tr');for(const v of row){const td=el('td');td.append(v instanceof Node?v:document.createTextNode(String(v??'—')));tr.append(td);}body.append(tr);}if(!rows.length){const tr=el('tr'),td=el('td','No records.');td.colSpan=headers.length;tr.append(td);body.append(tr);}t.append(body);wrap.append(t);return wrap;};
 const stateBadge=(el,s)=>el('span',String(s||'unknown').replaceAll('_',' '),'admin-state '+String(s||'unknown'));
 const fact=(el,label,value)=>{const x=el('div',undefined,'v5-fact');x.append(el('small',label),el('strong',String(value??'—')));return x;};
 async function analytics(o){
  const {post,action,el,container,title}=o;title.textContent='Analytics';const days=o.state?.days||30,data=await post('panel/admin-overview',{days});container.replaceChildren();
  const bar=el('div',undefined,'admin-command'),txt=el('div');txt.append(el('span','ADMIN ANALYTICS','admin-command-eyebrow'),el('h2','Today, volume and operational health'),el('p','Same V5 analytics view backed by the scoped Admin overview endpoint.'));const select=el('select');for(const n of [7,30,60]){const op=el('option','Last '+n+' days');op.value=n;select.append(op);}select.value=days;select.onchange=()=>action(()=>analytics({...o,state:{days:Number(select.value)}}));bar.append(txt,select);container.append(bar);
  const p=el('div',undefined,'admin-primary-kpis');for(const [l,v,h] of [['Today volume',money(data.todayVolume),'Collection + payout'],['Success rate',data.successRate==null?'—':(Number(data.successRate)*100).toFixed(1)+'%','India-day gateway success'],['Running UPI',data.runningUpi,'Active receiving routes'],['Active Users',data.activeUsers,'Approved + active'],['Today collection',money(data.todayCollection),'Successful pay-ins'],['Today payout',money(data.todayPayoutVolume),'Successful payouts']])p.append(card(el,l,v,h));container.append(p);
  const secondary=el('div',undefined,'admin-secondary-kpis');for(const [l,v] of [['Platform fees',money(data.totalFees)],['User commission',money(data.totalUserCommission)],['User deposits',money(data.totalUserDeposits)],['Payouts',data.successfulPayouts],['Users',data.totalUsers],['Merchants',data.totalMerchants],['Employees',data.totalEmployees],['Available UPI',data.availableUpi]]){const x=el('div');x.append(el('small',l),el('strong',String(v??'—')));secondary.append(x);}container.append(secondary);
  const grid=el('div',undefined,'admin-columns'),trend=el('section',undefined,'card admin-panel'),queue=el('section',undefined,'card admin-panel');trend.append(el('h2','Period activity'));for(const row of data.series||[]){const item=el('div',undefined,'admin-profit-row');item.append(el('span',row.day+' · '+row.kind),el('strong',money(row.amount)));trend.append(item);}queue.append(el('h2','Action queues'));for(const [l,v] of Object.entries(data.approvals||{})){const item=el('div',undefined,'admin-profit-row');item.append(el('span',l.replaceAll('_',' ')),el('strong',String(v??'—')));queue.append(item);}grid.append(trend,queue);container.append(grid);
 }
 async function approvals(o){
  const {post,el,container,title,navigate}=o;title.textContent='Pending approvals';container.replaceChildren();const [users,merchants,banks,deposits,payouts]=await Promise.all([
   post('panel/directory',{type:'user',status:'pending',search:'',offset:0}),
   post('panel/directory',{type:'merchant',status:'pending',search:'',offset:0}),
   post('business/banks',{}),
   post('funding/list',{state:'review',offset:0}),
   post('payout/approval/search',{offset:0,limit:25}).catch(()=>({orders:[]}))
  ]);
  const grid=el('div',undefined,'admin-primary-kpis');for(const [l,v,h] of [['User approvals',users.rows.length,'Registration review'],['Merchant approvals',merchants.rows.length,'Commercial activation'],['Bank / UPI review',banks.banks.filter(x=>['submitted','review'].includes(x.status)).length,'UPI review'],['Deposit review',deposits.requests.length,'Funding evidence'],['Payout approvals',(payouts.orders||[]).length,'Reserved payouts'],['Total waiting',users.rows.length+merchants.rows.length+banks.banks.filter(x=>['submitted','review'].includes(x.status)).length+deposits.requests.length+(payouts.orders||[]).length,'Visible scoped queues']])grid.append(card(el,l,v,h));container.append(grid);
  const list=el('section',undefined,'card admin-panel');list.append(el('h2','Open queues'));for(const [label,dest,count]of [['Users','administration.users',users.rows.length],['Merchants','administration.merchants',merchants.rows.length],['Bank & UPI','administration.bank-upi',banks.banks.filter(x=>['submitted','review'].includes(x.status)).length],['Deposits','administration.deposits',deposits.requests.length],['Payout approvals','payout.orders',(payouts.orders||[]).length]]){const r=el('div',undefined,'admin-review');r.append(el('span',label),el('strong',String(count)),button(el,'Review →',()=>navigate(dest)));list.append(r);}container.append(list);
 }
 async function upiAnalytics(o){
  const {request,post,el,container,title}=o;title.textContent='UPI Analytics';const [banks,routing]=await Promise.all([request('business/banks'),request('business/routing')]);container.replaceChildren();
  const running=banks.banks.filter(b=>b.status==='running'&&!b.frozen&&!b.deactivated),available=running.filter(b=>BigInt(b.daily_limit_minor||0)>0n);const grid=el('div',undefined,'admin-primary-kpis');for(const [l,v,h]of [['Total UPI',banks.banks.length,'Configured accounts'],['Running UPI',running.length,'Operational'],['Available UPI',available.length,'Limit configured'],['Routing candidates',routing.candidates.length,'Merchant/User eligibility'],['Reservations',routing.reservations.length,'Recent routing reservations'],['Reconciliation',routing.reconciliation.length,'Capacity deficit events']])grid.append(card(el,l,v,h));container.append(grid);
  const rows=banks.banks.map(b=>{const owner=b.owner_id,links=routing.candidates.filter(c=>c.userId===owner),eligible=links.filter(c=>c.eligible).length;return [b.details?.upiId||b.id,b.details?.bankName||'—',b.status,money(b.daily_limit_minor||0),String(eligible)+' / '+links.length,String(b.statement?.status||'—'),String(b.verification?.status||'—')];});container.append(table(el,['UPI','Bank','State','Daily limit','Eligible routes','Statement','Verification'],rows));
 }
 async function parkingView(o,mode){
  const {request,post,action,el,container,title}=o,data=await request('parking/admin');title.textContent=mode==='beneficiaries'?'Parking Beneficiaries':mode==='orders'?'Parking Orders':'Parking Review';container.replaceChildren();
  if(mode==='beneficiaries'){
   const top=el('div',undefined,'admin-toolbar');top.append(el('p','Admin/authorized Employee beneficiaries shown exactly as Users will receive them.','notice'));if(data.canCreate)top.append(button(el,'+ Create beneficiary',()=>createBeneficiary()));container.append(top);
   container.append(table(el,['Name','Workspace','Bank','Account','IFSC','UPI'],data.beneficiaries.map(b=>[b.details.beneficiaryName,b.tenantId,b.details.bankName,b.details.accountNumber,b.details.ifsc,b.details.upiId||'—'])));
   function createBeneficiary(){const d=document.createElement('dialog'),f=document.createElement('form');d.append(el('h2','Create Parking Beneficiary'));const mk=(label)=>{const l=el('label',label),i=el('input');i.required=true;l.append(i);f.append(l);return i;},tenant=mk('Workspace'),name=mk('Beneficiary name'),bank=mk('Bank name'),account=mk('Account number'),ifsc=mk('IFSC'),upi=mk('UPI ID');tenant.value=data.tenants[0]||'';const save=el('button','Create','primary');save.type='submit';f.append(save,button(el,'Cancel',()=>d.close()));f.onsubmit=e=>{e.preventDefault();action(async()=>{await post('parking/beneficiary/create',{requestId:crypto.randomUUID(),tenantId:tenant.value,beneficiaryName:name.value,bankName:bank.value,accountNumber:account.value,ifsc:ifsc.value.toUpperCase(),upiId:upi.value});d.close();await parkingView(o,mode);});};d.append(f);container.append(d);d.showModal();}
   return;
  }
  if(mode==='orders'){
   const top=el('div',undefined,'admin-toolbar');top.append(el('p','Total, minimum and maximum per transaction are enforced by the existing Parking backend.','notice'));if(data.canCreate)top.append(button(el,'+ Create Parking order',()=>createOrder()));container.append(top);
   container.append(table(el,['Reference','Workspace','Total','Minimum','Maximum','State'],data.orders.map(x=>[x.reference,x.tenantId,money(x.totalMinor),money(x.minMinor),money(x.maxMinor||x.totalMinor),x.state])));
   function createOrder(){const d=document.createElement('dialog'),f=document.createElement('form');d.append(el('h2','Create Parking Order'));const choose=(label,items)=>{const l=el('label',label),s=el('select');for(const [v,t]of items){const op=el('option',t);op.value=v;s.append(op);}l.append(s);f.append(l);return s;},inp=label=>{const l=el('label',label),i=el('input');i.required=true;l.append(i);f.append(l);return i;};const tenant=choose('Workspace',(data.tenants||[]).map(x=>[x,x])),beneficiary=choose('Beneficiary',data.beneficiaries.map(b=>[b.id,b.details.beneficiaryName+' · '+b.details.bankName])),reference=inp('Reference'),total=inp('Total INR'),min=inp('Minimum / txn INR'),max=inp('Maximum / txn INR'),toMinor=v=>{const [w,d='']=v.split('.');return (BigInt(w)*100n+BigInt(d.padEnd(2,'0'))).toString();};const save=el('button','Create','primary');save.type='submit';f.append(save,button(el,'Cancel',()=>d.close()));f.onsubmit=e=>{e.preventDefault();action(async()=>{await post('parking/order/create',{requestId:crypto.randomUUID(),tenantId:tenant.value,beneficiaryId:beneficiary.value,reference:reference.value,totalMinor:toMinor(total.value),minMinor:toMinor(min.value),maxMinor:toMinor(max.value)});d.close();await parkingView(o,mode);});};d.append(f);container.append(d);d.showModal();}return;
  }
  container.append(table(el,['Reference','User','Amount','State','Scan','Action'],data.reviews.map(r=>{const actions=el('div',undefined,'admin-row-actions');actions.append(button(el,'Proof',async()=>{const p=await post('parking/proof',{id:r.id});const url=URL.createObjectURL(new Blob([Uint8Array.from(atob(p.data),c=>c.charCodeAt(0))]));const a=document.createElement('a');a.href=url;a.download=p.name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}),button(el,'Approve',()=>decision(r,'approve'),'primary'),button(el,'Not paid',()=>decision(r,'not_paid'),'danger'));return [r.reference,r.userName,money(r.amountMinor),r.state,r.scanState||'—',actions];}));
  function decision(r,chosen){const d=document.createElement('dialog'),f=document.createElement('form'),l=el('label','Reason'),i=el('input');i.required=true;l.append(i);f.append(l);const save=el('button',chosen==='approve'?'Approve paid':'Not paid',chosen==='approve'?'primary':'danger');save.type='submit';f.append(save,button(el,'Cancel',()=>d.close()));f.onsubmit=e=>{e.preventDefault();action(async()=>{await post('parking/review',{id:r.id,action:chosen,reason:i.value});d.close();await parkingView(o,mode);});};d.append(el('h2','Parking decision'),f);container.append(d);d.showModal();}
 }
 async function pairingHistory(o){
  const {post,action,el,container,title}=o;title.textContent='Pairing History';const data=await post('operations/pairing-history',{offset:o.state?.offset||0});container.replaceChildren(el('p','Account-owned activation codes and their used / pending / expired / revoked state.','notice'));
  const rows=data.requests.map(r=>{const actions=el('div',undefined,'admin-row-actions');if(r.canCheck)actions.append(button(el,'Check',()=>action(()=>post('operations/device-setup/poll',{requestId:r.id}))));if(r.canRevoke)actions.append(button(el,'Revoke',()=>action(async()=>{await post('operations/device-setup/revokeCode',{requestId:r.id});await pairingHistory(o);}),'danger'));return [new Date(r.created_at).toLocaleString('en-IN'),r.owner_name,r.state,r.device_ref||'—',new Date(r.expires_at).toLocaleString('en-IN'),actions];});container.append(table(el,['Created','Owner','State','Device','Expires','Action'],rows));
 }
 async function render(destination,o){
  if(destination==='v5.analytics')return analytics(o);
  if(destination==='v5.approvals')return approvals(o);
  if(destination==='v5.upi-analytics')return upiAnalytics(o);
  if(destination==='v5.parking-beneficiaries')return parkingView(o,'beneficiaries');
  if(destination==='v5.parking-orders')return parkingView(o,'orders');
  if(destination==='v5.parking-review')return parkingView(o,'review');
  if(destination==='v5.pairing-history')return pairingHistory(o);
  throw new Error('error.NOT_FOUND');
 }
 root.WPayAdminV5Pages={render};
})(globalThis);
